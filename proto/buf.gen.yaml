version: v2

plugins:
  - remote: buf.build/protocolbuffers/go
    out: ../transport/grpc/pb
    opt:
      - paths=source_relative
  - remote: buf.build/grpc/go
    out: ../transport/grpc/pb
    opt:
      - paths=source_relative
      - require_unimplemented_servers=true

managed:
  enabled: true
